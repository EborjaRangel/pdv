[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && npm run db:push && npm run db:seed"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
