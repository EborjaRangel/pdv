# Railway API — desde la raíz del repo

[build]
builder = "NIXPACKS"
buildCommand = "npm install --prefix backend && npm run build --prefix backend && rm -rf backend/dist"

[deploy]
startCommand = "node scripts/railway-start.mjs"
restartPolicyType = "ON_FAILURE"
